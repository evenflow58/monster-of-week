{
  "name": "auth",
  "version": "1.0.0",
  "description": "The auth endpoints for the api",
  "main": "index.js",
  "scripts": {
    "compile": "tsc",
    "local-dev": "npm run compile && sam local start-api",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Evan Johnson",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.102",
    "@types/node": "^18.7.2",
    "aws-sdk": "^2.1194.0",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "googleapis": "^105.0.0"
  }
}
